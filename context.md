# 공유 컨텍스트 (context.md)

## 1. 비즈니스 도메인 용어 정의
* **svlabo**: 섀도우버스 포스트 및 덱 리스트를 수집하는 외부 사이트 (`svlabo.jp`).
* **표준 덱 (Standard Deck)**: 가중 평균 사용량을 기반으로 40장에 맞게 산출한 지능형 추천 덱 구성.
* **평균 사용량 (Average Usage)**: 날짜, 레이팅, 연승수 등을 반영한 통합 가중치 평균 카드 수량.
* **제거 가능성 (Removability Score)**: 덱 장수를 줄일 때 통계적 페널티가 가장 적은 카드의 우선순위 점수.
* **추가 가능성 (Addability Score)**: 덱 장수를 늘릴 때 통계적 페널티가 가장 적은 카드의 우선순위 점수.
* **통합 가중치 (Integrated Weight)**: 포스트의 업로드 일자(반감기 적용)와 레이팅(점수 가중)을 결합하여 분석 데이터에 적용하는 계산 가중치.
* **카드 데이터베이스 (Card Database)**: `card_database.json`에 저장된 카드명과 카드 ID 간의 매핑 데이터.
* **덱 코드 (Deck Code)**: `shadowverse-wb.com` 등 게임 내 덱 연동을 위해 카드 ID를 커스텀 base64 형태로 변환한 공유 URL.

## 2. 아키텍처 결정 사항 (ADR)
* **ADR-001: Selenium 단일 드라이버 인스턴스 재사용**
  * **결정**: Flask 서버 기동 시 하나의 WebDriver 인스턴스만 생성하여 요청 간 재사용.
  * **이유**: 매 요청마다 브라우저를 띄우면 10초 이상의 대기 시간이 발생하므로 응답 속도를 개선하기 위해 싱글톤 패턴 적용.
* **ADR-002: Docker 컨테이너 기반 Google Cloud Run 배포**
  * **결정**: Chrome 브라우저와 `chromedriver`를 함께 패키징하는 Docker 이미지를 작성하여 Cloud Run에 배포.
  * **이유**: GCP Cloud Run은 유연한 오토스케일링과 컨테이너 환경을 제공하여 Selenium 실행을 위한 최적의 서버리스 옵션임. (현재 온라인 배포 작업은 일시중지 상태)
* **ADR-003: NumPy 기반의 Penalty-Minimization 덱 매수 조정 알고리즘**
  * **결정**: `adjust_deck_count` 함수에서 덱 크기가 40장을 벗어날 경우 가중치와 표준편차를 기반으로 페널티를 최소화하는 카드 매수를 동적으로 가감.
* **ADR-004: 웹 프레임워크로 Flask 유지**
  * **결정**: 추가적인 비동기 전환 비용 및 템플릿 통합 단순성을 위해 기존 동기식 웹 프레임워크인 Flask를 유지.
* **ADR-005: JSON 기반 카드 데이터베이스 구조 유지**
  * **결정**: 가벼운 데이터 조회를 유지하기 위해 DB 서버를 구축하지 않고 로컬 `card_database.json` 파일 기반의 메모리 로딩 및 조회를 유지.
